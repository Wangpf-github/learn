1.是否有创建线程的必要，可以使用glib接口例如g_timeout_add(间隔一段时间执行回调函数)
2.如果必须使用线程，注意线程间公用变量的互斥锁
3.Atomic Operations：原子操作，glib提供的接口，可以实现类似加锁功能

4.关于指针使用问题，尽量少使用指针，避免出现一端释放掉后的空指针情况
5.关于命名问题，可参照GObject命名规则
6.关于资源释放问题，程序结束时释放掉不用的资源